# Runtime deps for the Cloud Run container.
# IMPORTANT: `adk deploy cloud_run` installs from THIS file (in the agent source
# dir), NOT from pyproject.toml. Any package the agent imports at runtime must be
# listed here, or the container 500s with ModuleNotFoundError on /run.
google-adk>=1.31.1,<2.0.0
firecrawl-py>=4.28.2
python-dotenv>=1.0.0
httpx>=0.27.0
pydantic>=2.0.0
google-genai>=1.40.0
google-cloud-logging>=3.12.0,<4.0.0
